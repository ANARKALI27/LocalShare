# Phase 2: GUI + drag/drop
PySide6>=6.7

# Phase 3: HTTP server
fastapi>=0.110
uvicorn>=0.29

# Phase 5: file uploads (required for multipart/form-data parsing)
python-multipart>=0.0.9

# Phase 6: WebDAV / Explorer integration (optional, experimental — see README)
wsgidav>=4.3
cheroot>=10.0

# Phase 8: QR code for phone access
qrcode[pil]>=7.4

# v2: internet sharing via Cloudflare Quick Tunnel (optional — see README)
# No Python package needed — requires the "cloudflared" program
# installed separately and on PATH (not pip-installable; see
# app/server/tunnel.py for install instructions per OS).
# wsgidav>=4.3          # Phase 6: WebDAV / Explorer integration
# qrcode[pil]>=7.4       # Phase 8: QR code
# zeroconf>=0.132        # Phase 8: LAN discovery
# pyinstaller>=6.6        # Phase 10: packaging
