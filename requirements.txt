# Phase 2: GUI + drag/drop
PySide6>=6.7

# Phase 3: HTTP server
fastapi>=0.110
uvicorn>=0.29

# Phase 5: file uploads (required for multipart/form-data parsing)
python-multipart>=0.0.9

# Phase 6: WebDAV / Explorer integration (optional, experimental — see README)
wsgidav>=4.3
cheroot>=10.0

# Phase 8: QR code for phone access
qrcode[pil]>=7.4

# v2: internet sharing via ngrok (optional — see README). Unlike the
# Cloudflare Quick Tunnel this replaced, pyngrok manages the actual
# ngrok binary itself automatically (downloads it on first use), so
# no separate install step is needed the way cloudflared required —
# just a free ngrok account and authtoken, configured in Settings.
pyngrok>=7.0
