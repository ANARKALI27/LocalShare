# Phase 2: GUI + drag/drop
PySide6>=6.7

# Phase 3: HTTP server
fastapi>=0.110
uvicorn>=0.29

# Phase 5: file uploads (required for multipart/form-data parsing)
python-multipart>=0.0.9

# Phase 6: WebDAV / Explorer integration (optional, experimental — see README)
wsgidav>=4.3
cheroot>=10.0

# Phase 8: QR code for phone access
qrcode[pil]>=7.4

# v2/v3: internet sharing via Cloudflare Quick Tunnel (optional — see
# README). No separate Python package needed — cloudflared (the actual
# tunnel program) is downloaded automatically at runtime using only
# the standard library (see app/server/tunnel.py's download_cloudflared),
# the first time Global mode is used and it isn't already installed.
